export interface ITextareaProps {
  name: string;
  label: string;
  inputProps?: any;
  value?: string;
  onChange: any;
  onBlur: any;
  width: string;
}
export interface ITextareaError {
  error?: boolean;
}
